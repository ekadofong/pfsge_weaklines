import setuptools

setuptools.setup (
    name = 'pfsge_weaklines',
    version='0.0.1',
    author='Erin Kado-Fong [w. code from Sean Johnson]',
    description='Basic empirical predictions for weak lines of interest to PFS GE',
    packages=setuptools.find_packages(),
    python_requires='>=3.7'
    )
